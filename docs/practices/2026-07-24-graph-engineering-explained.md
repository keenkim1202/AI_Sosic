---
title: Graph Engineering 해설 — 언제 쓰고 언제 쓰지 말아야 하나
source: https://x.com/AnatoliKopadze/status/2080668775796314331
author: Anatoli Kopadze (@AnatoliKopadze)
published: 2026-07-24
collected: 2026-08-10
tags: [claude-code, workflow, multi-agent, orchestration, cost, verification]
---

출처: [Graph Engineering explained: what it is, when to use it and when not to — @AnatoliKopadze](https://x.com/AnatoliKopadze/status/2080668775796314331) (X 롱폼 아티클)

## 요약

[Graph Engineering with Claude](./2026-07-20-graph-engineering-with-claude.md)와 같은 주제인데, 이 글이 채우는 건 **비용과 한계**임. 가짜 엣지 테스트, 다이아몬드 패턴 같은 기본은 겹치고, 여기만 있는 건 그래프가 깨지는 지점 3가지, 앵커 개념, 그래프를 **쓰지 말아야 할** 5가지 경우, 그리고 Bun 재작성에 실제로 든 **약 $165,000**이라는 청구서임.

## 겹치는 부분

두 글의 공통 기반은 짧게만.

- 노드는 일, 엣지는 흐르는 것. 엣지는 실제로 데이터가 지날 때만 존재함
- 가짜 엣지 테스트: 이 단계가 앞 단계 결과를 정말 읽는가? 아니면 대기는 낭비
- 선형 워크플로는 가장 슬픈 형태의 그래프임
- 다이아몬드(fan out → reduce → synthesize). 저자는 "올해 필요한 거의 유일한 패턴"이라고 씀

⚠️ 마지막 항목은 저자 주장으로 읽어야 함. [원조 글](./2026-07-20-graph-engineering-with-claude.md)은 `pipeline()` 대 `parallel()` 선택, 런타임 조건부 라우팅, loop-until-dry를 **각각 따로 필요한 토폴로지**로 설명함. 다이아몬드는 대표적인 패턴이지 유일한 패턴이 아님.

이 글이 보태는 수치가 하나 있음. **40단계 선형 워크플로는 순차 실패점이 40개고 지연시간이 40개의 합**이지만, 같은 40개 작업을 그래프로 그리면 실제 의존성은 보통 3~5개뿐이라 가장 느린 레이어 속도로 끝남. 저자는 5분짜리가 15초가 된다는 예를 듦. 다만 **이 배수는 저자 예시고 산술로 유도되지 않음.** 40개가 3~5개 레이어로 접힌다는 것만으로 20배가 나오지는 않고, 노드별 실행시간과 레이어당 병렬 폭을 알아야 계산됨. 방향만 취할 것. 저자 표현대로 **병목은 모델이 아니라 네가 그린 선**이었음.

## 검증자가 전부다

여기서 이 글이 원조 글보다 한 발 더 나감.

AI 자기 검토를 테스트한 결과는 다 같음. **모델은 자기 실수를 대부분 못 잡음.** 자기 작업을 채점하는 모델은 자신에게 지나치게 관대함. 그래서 일한 에이전트가 그 일을 검사하게 하지 않음.

그런데 아무도 이름 붙이지 않는 함정이 있음. **검증자는 깨끗한 컨텍스트가 필요함.**

> 워커가 쓰던 대화를 그대로 주면 검사하는 게 아니라, 폰트만 바꿔서 자기한테 고개를 끄덕이는 것임. 컨텍스트를 공유하는 에이전트 그래프는 코스튬 입은 단일 루프고, 똑같이 깨지는데 더 늦게 더 비싸게 깨짐.

그리고 검사를 세 갈래로 나눔. **맞는가, 최신인가, 출처가 실재하는가.** 서로 다른 세 렌즈가 동일한 열 개보다 많이 잡음.

기억할 규칙은 하나. **워커와 그 검증자는 절대 컨텍스트를 공유하지 않는다.**

## 그래프가 깨지는 3가지

**1. 컨텍스트 붕괴.** 노드 천 개로 팬아웃한 다음 천 개 출력을 마지막 단계 하나에 다 넣으려 하면, 합성 시작 전에 컨텍스트 윈도우를 넘김. 해법은 팬인을 계층화하는 것. 결과를 배치로 묶고 배치마다 요약한 뒤 요약을 합침. 원시 더미를 합치지 않음.

**2. 거짓 독립.** 두 노드가 프롬프트에서 서로를 언급하지 않으니 독립처럼 보이는데, 둘 다 같은 파일에 쓰거나 같은 rate-limited API를 때림. 그게 **숨은 엣지**임. Bun 팀이 처음 큰 작업을 여러 에이전트로 팬아웃했을 때 워크스페이스를 공유해서 서로를 덮어썼음. 해법은 워커마다 격리 공간을 주고, 공유 데이터만이 아니라 **공유 리소스**를 감사하는 것.

**3. 조용한 노드 실패.** 사슬에서는 실패 하나가 전부를 멈춰서 짜증나지만 명백함. 그래프에서는 200개 중 죽은 노드 하나가 완성돼 보이는 리포트에 슬쩍 들어감. 해법은 모든 머지 단계가 **입력 개수를 기대값과 대조해서 차이를 플래그**하는 것. 절반의 데이터로 조용히 진행하지 않게.

## 앵커 — 아무도 듣고 싶어 하지 않는 부분

이 글의 진짜 기여가 여기임.

전체 그래프를 다 만들었다고 상상해보라. 짝지어진 검증자, 감사 노드, 다른 노드를 조정하는 메타 노드. 모든 노드가 다른 노드를 감시하고, 전부가 리포트를 읽음. 감사 노드는 숫자를 재무 숫자와 대조하는데, 그 재무 숫자도 애초에 같은 시스템에서 나왔음.

> 모든 게 일관됨. 아무것도 검증되지 않음.

이 그래프는 단일 루프와 정확히 같은 방식으로 실패함. 더 늦게, 더 비싸게, 내려가는 길에 초록불을 훨씬 많이 켜면서.

**토폴로지만으로는 진실을 살 수 없음.** 그래프에는 앵커가 필요함. 논쟁할 수 없는 노드들.

- "통과해야 함"이 아니라 **실제로 돌아서 통과한** 테스트
- 은행에 **실제로 들어온** 매출
- **실제로 남은** 고객

그리고 일부 규칙은 얼려야 함. 최적화기가 약화시키고 싶어할 규칙들, 바로 그 이유로 손댈 수 없게 둬야 하는 것들. **그래프는 그 안에서 움직이기를 거부하는 것들만큼만 정직함.**

## 그래프가 필요 없는 경우

저자가 자기 글 관례라며 솔직하게 정리한 부분.

> 그래프는 **폭**을 사는 것이지 **판단**을 사는 게 아님.

이럴 때는 건너뜀.

- 작업이 작거나 고립돼 있음. 함수 하나 추가, 버그 하나 수정. 조율은 순수 오버헤드고 단일 에이전트가 더 빠르고 싸다
- 모든 단계를 승인하고 싶음. 그래프의 요점은 사람 없이 넓게 도는 건데, 짧은 목줄은 그 목적과 싸움
- 아직 무엇을 찾는지 모름. 탐색적 작업은 조종할 수 있는 에이전트 하나를 원함. 계획에 묶인 함대가 아님
- 단계들이 진짜로 서로 의존함. 순차 작업에 그래프를 억지로 씌우면 속도 이득 0에 비용만 추가
- 판별법은 가짜 엣지 테스트. **엣지 없는 두 작업을 못 찾으면 만들 그래프가 없음.** 그건 루프고, 루프도 괜찮음

## 비용 — Bun 재작성 사례

가장 중요한 숫자들이 여기 있음. 저자가 공개 사례라며 든 것.

| 항목 | 값 |
|---|---|
| 작업 | Bun 런타임 재작성. 한 언어 약 535,000줄 → 다른 언어 100만 줄 이상 |
| 기간 | 약 11일 (수작업이면 1년 가까이) |
| 워크플로 실행 | 약 50회 |
| 동시 에이전트 | 최대 64개 |
| 사용 비용 | **약 $165,000** |

그리고 사람이 전체를 설계하고 감시해야 했고, 그만큼의 AI 작성 코드를 안전하게 리뷰할 수 있느냐에 대한 실제 비판을 받았음.

> 그래프는 천 개 에이전트로 팬아웃해서 단일 컨텍스트가 담을 수 없는 작업을 씹어낼 수 있음. 동시에 엉뚱한 작업을 가리키거나 앵커를 빼먹으면 백그라운드에서 조용히 돈을 쓸 수도 있음.

무거운 버전은 예산과 상한과 모니터링을 갖춘 팀 것임. 아니면 작게 시작해서 한 번 실행에 얼마 드는지 보고, 그게 값어치를 증명한 뒤에 넓히면 됨.

## 배경

한 달 전엔 필드 전체가 루프를 이야기하고 있었음. Peter Steinberger의 게시물 하나로, 루프를 막 배운 인터넷 한 구석이 하룻밤에 루프를 구식으로 선언했음. 농담이 먹힌 건 절반은 사실이었기 때문.

옮겨간 게 더 나은 루프는 아님. **루프의 그래프**임. 에이전트 하나가 숫자 하나를 홀로 쫓는 대신, 순환들이 서로를 감시하고 교정하는 네트워크.

엔지니어들은 몇 시간 안에 이게 수십 년 된 아이디어에 새 이름을 붙인 거라고 반박했음. 저자는 그게 맞고, 그게 오히려 좋은 소식이라고 함. **30년간 크리티컬 시스템을 돌려온 패턴이야말로 내 작업을 맡기고 싶은 것**이니까.

## 저자의 결론

- 강점은 폭. 독립적인 작업을 동시에 처리하는 것
- 약점은 폭을 살 뿐 판단을 사지 않고, 엉뚱한 일을 시키면 돈을 쓴다는 것
- 그러니 모든 걸 그래프로 만들 게 아니라, **작업이 그래프를 필요로 할 만큼 넓은지 아는 것**이 핵심
- 오늘 밤 할 일: 가짜 엣지 테스트를 배우고, 지금 워크플로를 그려서 데이터를 나르지 않는 엣지를 지운다

## 메모

- 저자가 X와 Telegram 홍보를 본문에 여러 번 넣음. 판단은 대체로 균형 잡혀 있지만 그 부분은 걸러 읽으면 됨
- Bun 사례의 $165,000은 이 글이 인용한 공개 사례 수치임. 원출처를 직접 확인하지는 않았음
- [원조 그래프 엔지니어링 글](./2026-07-20-graph-engineering-with-claude.md)이 "어떻게 만드나"라면 이 글은 "언제 만들지 말아야 하나"임. 둘을 같이 읽는 게 맞고, 굳이 하나만 골라야 하면 이쪽이 실무 판단에 더 유용함

## 관련 문서

- [Graph Engineering with Claude](./2026-07-20-graph-engineering-with-claude.md) — 같은 주제의 14단계 구현 가이드
- [Eval Engineering](./2026-08-01-eval-engineering-merge-gate.md) — 여기서 말하는 앵커를 머지 게이트로 제도화한 이야기
- [Prompt Caching In Agents](../agents/2026-07-22-prompt-caching-in-agents.md) — 에이전트 함대의 청구서가 어디서 불어나는지
- [Harness · Loop · Graph](./2026-07-28-loop-graph-harness.md) — 여기서 말하는 앵커를 세 계층 관점으로 재배치. graph 앞에 loop가 와야 하는 이유
