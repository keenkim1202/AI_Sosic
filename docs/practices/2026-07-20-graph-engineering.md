---
title: Graph Engineering, 14단계 구현과 쓰지 말아야 할 5가지
source: https://x.com/0xCodez/status/2079165300625330317
author: Codez (@0xCodez), Anatoli Kopadze (@AnatoliKopadze)
published: 2026-07-20
collected: 2026-08-10
tags: [claude-code, workflow, multi-agent, orchestration, parallel, subagent, cost, verification]
---

출처: [Graph Engineering with Claude: 14-Step roadmap from 0 to graph architect, @0xCodez](https://x.com/0xCodez/status/2079165300625330317) (2026-07-20) · [Graph Engineering explained: what it is, when to use it and when not to, @AnatoliKopadze](https://x.com/AnatoliKopadze/status/2080668775796314331) (2026-07-24)

## 요약

멀티스텝 에이전트를 만들면 대부분 직선이 나옴. 1단계, 2단계, 3단계가 앞 단계 끝나기를 얌전히 기다림. 근데 그중 절반은 애초에 기다릴 이유가 없었음. 앞쪽 절반은 그 일렬 종대를 그래프로 다시 그리는 **14단계 구현 가이드**(@0xCodez)고, 뒤쪽 절반은 같은 주제를 **비용과 한계** 쪽에서 본 해설(@AnatoliKopadze)임. 후자에만 있는 건 그래프가 깨지는 3가지, **앵커** 개념, 그래프를 **쓰지 말아야 할** 5가지 경우, 그리고 Bun 재작성에 든 **약 $165,000**이라는 청구서임.

Claude Code의 dynamic workflows를 전제로 쓰였고, 오케스트레이션이 대화가 아니라 코드라서 조율 자체엔 모델 토큰이 안 든다는 걸 지렛대로 씀.

## 핵심 전제

프롬프트는 문장이고 루프는 순환이고 하네스는 에이전트가 딛는 바닥임. 근데 작업 자체의 모양, 그러니까 무엇이 무엇보다 먼저 돌고 무엇이 동시에 돌 수 있고 무엇이 전부를 기다려야 하는지, **그 모양은 그래프임.** 노드가 생각하고 엣지가 결과를 나름.

## 01~04. 노드와 엣지 바로 세우기

**노드는 일, 엣지는 흐르는 것.** 노드는 에이전트 하나, 경계 분명한 일 하나, 입력 하나에 출력 하나. 엣지는 의존성이고, 이 노드의 출력이 저 노드의 입력으로 들어간다는 뜻임. 그게 전부.

흔한 실수가 "그리고 나서"를 엣지로 취급하는 것. "파일을 요약하고 나서 날씨를 알려줘"에는 둘 사이에 엣지가 없음. 날씨는 요약을 소비하지 않으니까. 자기 에이전트의 모든 "그리고 나서"에 물어봐야 함. 다음 단계가 앞 단계 출력을 읽는가? 아니면 엣지는 없고 그 대기는 낭비임. 이게 **가짜 엣지 테스트**고, 두 글이 공통으로 가장 강조하는 도구임.

**선형 스크립트는 퇴화한 그래프임.** A→B→C→D는 모든 노드가 들어오는 엣지 하나, 나가는 엣지 하나인 사슬임. 돌긴 돔. 느리고 취약할 뿐임. C가 멈추면 D는 영영 안 돌고 A의 작업은 갈 곳 없이 상류에 갇힘.

**노드마다 계약을 줌.** 추론할 수 없는 노드는 병렬화할 수 없는 노드임. 경계 있는 입력, 경계 있는 출력, 정확히 하나의 일. 워크플로에선 스키마로 강제함. `agent()` 호출에 JSON 스키마를 주면 서브에이전트가 검증된 구조화 데이터를 반환하도록 강제되고, 검증이 툴 호출 레이어에서 일어나니까 불일치 시 재시도함. 파싱하고 기도할 필요가 없음.

**엣지도 데이터 계약임.** "B가 A 다음"이 아니라 "A는 이 모양을 만들고 B는 이 모양을 소비한다"는 약속임. 순서가 아니라 데이터로 엣지에 이름을 붙이면 엣지가 진짜인지 즉시 보이고, 모양만 유지되면 양쪽 노드를 갈아끼울 수 있음. 팬아웃이랑 합성 사이의 reduce 단계, 즉 flatten·dedupe·filter는 그냥 코드임. 에이전트 필요 없음. 사람들이 모델 토큰 태우는 일의 상당수가 실은 엣지고, **엣지에는 모델 호출이 없으니 토큰 비용이 안 붙음.** (CPU·메모리·구현 비용은 당연히 듦)

## 05~07. 팬아웃, 팬인, 다이아몬드

**`parallel()`로 팬아웃.** 독립적인 노드가 N개면 체인하지 말고 한꺼번에 던짐. 두 가지 디테일이 이걸 견고하게 만듦. 첫째, `parallel()`은 배리어라서 모든 thunk를 기다린 뒤 반환함. 둘째, 예외를 던진 thunk는 배치 전체를 실패시키는 대신 null로 해소됨. 이건 이 워크플로 런타임의 계약이고 `Promise.all` 같은 일반 구현은 첫 예외에서 전체를 reject하니 옮겨 쓸 때 확인해야 함. 그러니 항상 `.filter(Boolean)`. 동시 실행은 코어 수 근처에서 제한되고 나머지는 큐에 들어가니까 백 개를 넘겨도 다 끝남.

여기가 진짜 이득 나는 지점임. 팬아웃이 모델 대화가 아니라 Claude가 쓴 코드 안에서 일어남. Claude의 컨텍스트는 소스 아홉 개를 동시에 안 들고 있음. 서브에이전트가 각자 자기 컨텍스트를 지고 최종 답만 돌려줌.

해설 글이 여기 보태는 관점이 하나 있음. **40단계 선형 워크플로는 순차 실패점이 40개고 지연시간이 40개의 합**이지만, 같은 40개 작업을 그래프로 그리면 실제 의존성은 보통 3~5개뿐이라 가장 느린 레이어 속도로 끝남. 저자는 5분짜리가 15초가 된다는 예를 듦. 다만 **이 배수는 저자 예시고 산술로 유도되지 않음.** 40개가 3~5개 레이어로 접힌다는 것만으로 20배가 나오지는 않고, 노드별 실행시간과 레이어당 병렬 폭을 알아야 계산됨. 방향만 취할 것. 저자 표현대로 **병목은 모델이 아니라 네가 그린 선**이었음.

**배리어에서 팬인.** 팬아웃은 뭔가가 모아줘야 쓸모가 있음. 전체 집합이 있어야 하는 일, 그러니까 소스 간 중복 제거, 영향도 랭킹, 총계가 0이면 조기 종료 같은 걸 할 때만 배리어가 벽시계 비용을 정당화함.

판별법이 단순함. `parallel → transform → parallel`을 썼는데 가운데 transform에 항목 간 의존성이 없으면, pipeline을 썼어야 하고 배리어는 건너뛰었어야 함.

**다이아몬드.** 팬아웃이랑 팬인을 합치면 나오는 주력 토폴로지임. 한 노드가 쪼개고, 여러 노드가 병렬로 일하고, 한 노드가 합침. 정식 이름은 fan out → reduce → synthesize. 시장 조사, 의존성 감사, 코드 리뷰, 리서치 리포트 전부 이 골격임. 다이아몬드가 보이기 시작하면 "에이전트가 더 많은 단계를 하게 하려면"이 아니라 "쪼개는 곳은 어디고 합치는 곳은 어디인가"를 묻게 됨.

## 08~11. 라우팅, 검증, 격리, 순환

**런타임 조건부 라우팅.** 라우터 노드가 결과를 보고 어느 하류 경로를 태울지 정함. 워크플로에선 검증된 출력에 대한 JavaScript `if`나 `switch`일 뿐임. 판단은 Claude가 하고 라우팅은 Claude가 쓴 코드가 함. 같은 분류에 대해 매번 같게 돔. "Claude가 감사를 건너뛰기로 했다" 같은 창발적 사고가 없음. 건너뛰려면 그래프에 쓰여 있어야 하는데 안 쓰여 있으니까.

**엣지 위에 검증자.** 그래프의 진짜 레버리지는 에이전트 수가 아니라 그 주위에 두를 수 있는 구조임. 검증자 노드는 결과가 하류로 가기 전에 앉아서, 그 발견을 **죽이는 것만을** 임무로 삼음.

- 적대적 검증: 발견마다 반박하도록 프롬프트된 독립 회의론자 N명을 띄우고, 과반이 살아남을 때만 통과
- 관점 다양화 검증: 검증자마다 다른 렌즈를 줌. 정확성, 보안, 재현 여부. 동일한 검사 N개가 못 잡는 실패 모드를 다양성이 잡음
- 심사 패널: 서로 다른 각도로 N개 시도를 만들고 병렬 심사자로 채점한 뒤, 승자를 기반으로 합성하되 차점자의 좋은 부분을 접붙임

**격리.** 사슬에선 실패가 연쇄함. 그래프에선 노드에 갇혀야 함. `parallel()` 안에서 예외는 null이 되니까 좋은 에이전트 여덟은 여전히 반환됨. `.filter(Boolean)`이 봉쇄 장치임. 모든 팬인은 입력이 빠질 수 있다고 가정하고 설계할 것.

더 미묘한 실패는 노드끼리 밟는 것. 병렬로 파일을 쓰면 충돌함. `isolation: "worktree"`로 각 에이전트가 자기 git worktree에서 일하고 깔끔히 머지하게 함. 노드가 실제로 병렬로 쓸 때만 꺼내야 함. 모든 실행에 부과하는 기본 세금이 아니라 그 한 토폴로지를 위한 안전벨트임.

**수렴하는 순환.** 규모를 미리 모르는 일이 있음. 버그 하나 찾으면 셋이 더 드러나는 스윕 같은 것. 순환이 필요한데 수렴 안 하는 순환은 예산 마를 때까지 에이전트를 뽑는 무한 루프임.

수렴하는 패턴은 loop-until-dry임. K회 연속으로 새로운 게 안 나올 때까지 파인더를 띄움. 성패를 가르는 디테일, 그리고 거의 모두가 처음에 틀리는 지점은 **무엇에 대해 중복 제거하느냐**임. 확인된 결과가 아니라 **본 것 전부**에 대해 중복 제거해야 함. 안 그러면 기각된 발견이 매 라운드 다시 나타나고, 루프는 영영 안 마르고, 같은 막다른 길을 돈 내고 재발견하는 기계를 만들게 됨.

## 12~14. 비용, 지연시간, 자동 설계

**모델 티어링.** 모든 노드에 최고 모델이 필요하진 않음. 경계 분명하고 반복적인 노드(필드 추출, 티켓 분류)랑 진짜 판단이 실리는 노드(리포트 합성, 발견 판정)를 나눔. 워크플로에서 서브에이전트는 기본적으로 세션 모델을 상속하니까, 큰 실행이 통째로 세션 티어로 청구됨. `agent()` 호출의 `model` 옵션으로 그 노드만 다른 데로 보냄.

**토폴로지가 곧 비용이자 지연시간.** 모두가 걸려 넘어지는 선택이 `parallel()` 대 `pipeline()`임. `parallel()` 배리어는 다음 단계 시작 전에 가장 느린 노드를 전부 기다리게 함. `pipeline()`은 항목마다 독립적으로 전 단계를 흘려보내니까, 항목 A가 3단계에 있는 동안 항목 B는 1단계에 있을 수 있음. 빠른 항목이 느린 항목 뒤에서 놀지 않고 먼저 끝남.

기본은 `pipeline()`. 배리어는 한 단계가 정말로 이전 결과 전부를 동시에 필요로 할 때만. "코드가 더 깔끔하다"랑 "단계가 개념적으로 분리돼 있다"는 이유가 안 됨. 분리는 동기화랑 다름.

**Claude한테 그래프를 그리게 하기.** 미리 계획할 수 없는 일까지 손으로 그리는 걸 그만두는 단계임. 목표를 서술하면 Claude가 오케스트레이션 스크립트를 직접 씀. 진입 경로는 셋. 프롬프트에 "workflow"라는 단어를 쓰거나, 저장된 워크플로를 돌리거나(`/deep-research`가 실제 프로덕션 그래프임. scope → parallel search → fetch → adversarial verify → synthesize), ultracode를 켜서 세션의 모든 실질 작업에 워크플로를 계획하게 하거나. 좋은 실행은 `s`를 눌러 `.claude/workflows/`에 저장하면 이름으로 재실행 가능한 버전 관리 자산이 됨.

## 검증자가 전부다

여기서부터가 해설 글이 한 발 더 나가는 부분임.

AI 자기 검토를 테스트한 결과는 다 같음. **모델은 자기 실수를 대부분 못 잡음.** 자기 작업을 채점하는 모델은 자신에게 지나치게 관대함. 그래서 일한 에이전트가 그 일을 검사하게 하지 않음.

그런데 아무도 이름 붙이지 않는 함정이 있음. **검증자는 깨끗한 컨텍스트가 필요함.**

> 워커가 쓰던 대화를 그대로 주면 검사하는 게 아니라, 폰트만 바꿔서 자기한테 고개를 끄덕이는 것임. 컨텍스트를 공유하는 에이전트 그래프는 코스튬 입은 단일 루프고, 똑같이 깨지는데 더 늦게 더 비싸게 깨짐.

그리고 검사를 세 갈래로 나눔. **맞는가, 최신인가, 출처가 실재하는가.** 서로 다른 세 렌즈가 동일한 열 개보다 많이 잡음.

기억할 규칙은 하나. **워커와 그 검증자는 절대 컨텍스트를 공유하지 않는다.**

## 그래프가 깨지는 3가지

**1. 컨텍스트 붕괴.** 노드 천 개로 팬아웃한 다음 천 개 출력을 마지막 단계 하나에 다 넣으려 하면, 합성 시작 전에 컨텍스트 윈도우를 넘김. 해법은 팬인을 계층화하는 것. 결과를 배치로 묶고 배치마다 요약한 뒤 요약을 합침. 원시 더미를 합치지 않음.

**2. 거짓 독립.** 두 노드가 프롬프트에서 서로를 언급하지 않으니 독립처럼 보이는데, 둘 다 같은 파일에 쓰거나 같은 rate-limited API를 때림. 그게 **숨은 엣지**임. Bun 팀이 처음 큰 작업을 여러 에이전트로 팬아웃했을 때 워크스페이스를 공유해서 서로를 덮어썼음. 해법은 워커마다 격리 공간을 주고, 공유 데이터만이 아니라 **공유 리소스**를 감사하는 것.

**3. 조용한 노드 실패.** 사슬에서는 실패 하나가 전부를 멈춰서 짜증나지만 명백함. 그래프에서는 200개 중 죽은 노드 하나가 완성돼 보이는 리포트에 슬쩍 들어감. 해법은 모든 머지 단계가 **입력 개수를 기대값과 대조해서 차이를 플래그**하는 것. 절반의 데이터로 조용히 진행하지 않게.

## 앵커, 아무도 듣고 싶어 하지 않는 부분

해설 글의 진짜 기여가 여기임.

전체 그래프를 다 만들었다고 상상해보라. 짝지어진 검증자, 감사 노드, 다른 노드를 조정하는 메타 노드. 모든 노드가 다른 노드를 감시하고, 전부가 리포트를 읽음. 감사 노드는 숫자를 재무 숫자와 대조하는데, 그 재무 숫자도 애초에 같은 시스템에서 나왔음.

> 모든 게 일관됨. 아무것도 검증되지 않음.

이 그래프는 단일 루프와 정확히 같은 방식으로 실패함. 더 늦게, 더 비싸게, 내려가는 길에 초록불을 훨씬 많이 켜면서.

**토폴로지만으로는 진실을 살 수 없음.** 그래프에는 앵커가 필요함. 논쟁할 수 없는 노드들.

- "통과해야 함"이 아니라 **실제로 돌아서 통과한** 테스트
- 은행에 **실제로 들어온** 매출
- **실제로 남은** 고객

그리고 일부 규칙은 얼려야 함. 최적화기가 약화시키고 싶어할 규칙들, 바로 그 이유로 손댈 수 없게 둬야 하는 것들. **그래프는 그 안에서 움직이기를 거부하는 것들만큼만 정직함.**

## 그래프가 필요 없는 경우

해설 글 저자가 자기 글 관례라며 솔직하게 정리한 부분.

> 그래프는 **폭**을 사는 것이지 **판단**을 사는 게 아님.

이럴 때는 건너뜀.

- 작업이 작거나 고립돼 있음. 함수 하나 추가, 버그 하나 수정. 조율은 순수 오버헤드고 단일 에이전트가 더 빠르고 싸다
- 모든 단계를 승인하고 싶음. 그래프의 요점은 사람 없이 넓게 도는 건데, 짧은 목줄은 그 목적과 싸움
- 아직 무엇을 찾는지 모름. 탐색적 작업은 조종할 수 있는 에이전트 하나를 원함. 계획에 묶인 함대가 아님
- 단계들이 진짜로 서로 의존함. 순차 작업에 그래프를 억지로 씌우면 속도 이득 0에 비용만 추가
- 판별법은 가짜 엣지 테스트. **엣지 없는 두 작업을 못 찾으면 만들 그래프가 없음.** 그건 루프고, 루프도 괜찮음

## 비용, Bun 재작성 사례

가장 중요한 숫자들이 여기 있음. 해설 글 저자가 공개 사례라며 든 것.

| 항목 | 값 |
|---|---|
| 작업 | Bun 런타임 재작성. 한 언어 약 535,000줄 → 다른 언어 100만 줄 이상 |
| 기간 | 약 11일 (수작업이면 1년 가까이) |
| 워크플로 실행 | 약 50회 |
| 동시 에이전트 | 최대 64개 |
| 사용 비용 | **약 $165,000** |

그리고 사람이 전체를 설계하고 감시해야 했고, 그만큼의 AI 작성 코드를 안전하게 리뷰할 수 있느냐에 대한 실제 비판을 받았음.

> 그래프는 천 개 에이전트로 팬아웃해서 단일 컨텍스트가 담을 수 없는 작업을 씹어낼 수 있음. 동시에 엉뚱한 작업을 가리키거나 앵커를 빼먹으면 백그라운드에서 조용히 돈을 쓸 수도 있음.

무거운 버전은 예산과 상한과 모니터링을 갖춘 팀 것임. 아니면 작게 시작해서 한 번 실행에 얼마 드는지 보고, 그게 값어치를 증명한 뒤에 넓히면 됨.

## 배경

한 달 전엔 필드 전체가 루프를 이야기하고 있었음. Peter Steinberger의 게시물 하나로, 루프를 막 배운 인터넷 한 구석이 하룻밤에 루프를 구식으로 선언했음. 농담이 먹힌 건 절반은 사실이었기 때문.

옮겨간 게 더 나은 루프는 아님. **루프의 그래프**임. 에이전트 하나가 숫자 하나를 홀로 쫓는 대신, 순환들이 서로를 감시하고 교정하는 네트워크.

엔지니어들은 몇 시간 안에 이게 수십 년 된 아이디어에 새 이름을 붙인 거라고 반박했음. 저자는 그게 맞고, 그게 오히려 좋은 소식이라고 함. **30년간 크리티컬 시스템을 돌려온 패턴이야말로 내 작업을 맡기고 싶은 것**이니까.

## 이번 주에 만들어볼 그래프 6개

- 전체 라우트 보안 스윕. 라우트 파일마다 서브에이전트 하나가 누락된 인증 체크를 사냥하고, 검증 패스가 리포트 전에 모든 발견을 확인
- `/deep-research`로 인용 달린 리포트. 질문을 여러 각도로 분해, 병렬 검색, 소스 중복 제거, 3표 회의론자로 모든 주장을 적대적 검증
- 모듈 파일 단위 포팅. 번역을 파일별로 팬아웃하고 각각에 테스트 스위트를 게이트로 걸고 실패를 되돌림
- diff 적대적 리뷰. diff 크기로 라우팅해서 작으면 빠른 패스, 크면 정확성·보안·성능 렌즈별 병렬 감사 후 심사 패널이 합성
- 스케줄 생태계 스캔. 한 번 저장하고 계속 재실행. 여러 소스를 병렬로 확인하고 배리어에서 영향도 순으로 랭킹한 뒤 다이제스트 작성
- 규모 미상 발견. 파인더를 병렬로 돌리고 새 발견을 본 것 전부와 대조해 중복 제거, 생존자를 검증, 두 라운드 연속 새 게 없으면 정지

## 결론

프롬프터는 질문을 하고 아키텍트는 그래프를 그림. 선형 에이전트는 천장이 아니라 첫 번째 모양이었을 뿐임. 우리가 타이핑하는 방식이랑 닮아서 모두가 먼저 집는 모양.

다만 강점은 폭이지 판단이 아님. 그러니 모든 걸 그래프로 만들 게 아니라, **작업이 그래프를 필요로 할 만큼 넓은지 아는 것**이 핵심임. 오늘 밤 할 일은 하나. 지금 워크플로를 그려서 데이터를 나르지 않는 엣지를 지우는 것.

## 메모

- 해설 글 저자는 다이아몬드를 "올해 필요한 거의 유일한 패턴"이라고 씀. 이건 저자 주장으로 읽어야 함. 14단계 쪽은 `pipeline()` 대 `parallel()` 선택, 런타임 조건부 라우팅, loop-until-dry를 **각각 따로 필요한 토폴로지**로 설명함. 다이아몬드는 대표적인 패턴이지 유일한 패턴이 아님
- Bun 사례의 $165,000은 해설 글이 인용한 공개 사례 수치임. 원출처를 직접 확인하지는 않았음
- 해설 글 저자가 X와 Telegram 홍보를 본문에 여러 번 넣음. 판단은 대체로 균형 잡혀 있지만 그 부분은 걸러 읽으면 됨
- 두 글은 원래 별도 문서였는데 겹치는 부분이 커서 하나로 합쳤음 (2026-08-21). 앞부분이 "어떻게 만드나", 뒷부분이 "언제 만들지 말아야 하나"임

## 관련 문서

- [Prompt Caching In Agents](../agents/2026-07-22-prompt-caching-in-agents.md): 서브에이전트를 대량으로 띄울 때 실제 청구가 어디서 발생하는지
- [Eval Engineering](./2026-08-01-eval-engineering-merge-gate.md): 여기서 말하는 검증자 노드와 앵커를 머지 게이트로 제도화한 이야기
- [Harness · Loop · Graph](./2026-07-28-loop-graph-harness.md): graph가 세 계층 중 어디에 앉는지, 그리고 graph를 너무 일찍 만들면 안 되는 이유
